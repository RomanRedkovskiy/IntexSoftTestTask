server:
  port: 8761

spring:
  application:
    name: eureka-server

eureka:
  client:
    registerWithEureka: false  # Disable self-registration
    fetchRegistry: false       # Disable fetching registry
    serviceUrl:
      defaultZone: http://${HOST:localhost}:${EUREKA_PORT:8761}/eureka/